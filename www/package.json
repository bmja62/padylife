{
  "name": "padylife-www",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "prepare:staging": "cp ./.env.staging ./.env",
    "prepare:production": "cp ./.env.production ./.env",
    "build": "vite build",
    "preview": "vite preview --port 5050",
    "start": "serve -s dist -l ${PORT:-3000}"
  },
  "dependencies": {
    "vue": "^3.5.18",
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^5.4.19"
  }
}
